# سجل نشر النسخة التجريبية المجانية — 20 سبتمبر 2026

تم النشر بطلب المستخدم إلى المشاريع الحالية. لم يُفتح متصفح جديد ولم تُنشأ حسابات تجريبية أو تُرسل حملات إشعار.

## الويب ولوحة الإدارة

| العنصر | رابط الإنتاج | الحالة | معرّف النشر |
|---|---|---|---|
| التطبيق — Expo Web | https://akhoo-abad003-6200s-projects.vercel.app | READY، production | `dpl_uKgGr6nsDi7z3nTnwExQDGeqy1Dm` |
| الإدارة — Vite | https://akhoo-admin.vercel.app | READY، production | `dpl_CKgvTj3ejksm9TzYtx1XBkrGntw9` |

بُنيت النسختان محلياً بإعدادات الإنتاج ثم رُفعت المخرجات الجاهزة وتمت ترقيتهما إلى الإنتاج. عولج تكرار مجلد `admin` في إعدادات البناء المحلية دون تغيير جذر المشروع البعيد، وأُضيف `admin/vercel.json` لدعم فتح مسارات الإدارة مباشرة.

النشر تم من شجرة العمل المعدلة فوق `9943dc3e7c33cd79fdb56d07d2469345f3e59588` قبل إنشاء commit للإصلاحات؛ معرّف الأساس وحده لا يحتويها. تغييرات المصدر وسجل النشر محفوظة معاً في سجل Git، واعتمد المستخدم بعدها رفع التعديلات المكتملة إلى GitHub كجزء من إتمام العمل.

## Supabase

- المشروع: `zmpwdufahjmlxfwzafvr`، الاسم AKHOO، الحالة `ACTIVE_HEALTHY` عند الفحص.
- طُبقت migrations `0027`–`0031` بنجاح، وجرى التحقق أن آخر نسخة في قاعدة البيانات هي `0031`.
- الدوال الخمس نشطة: `delete-account` الإصدار 8، `send-broadcast-notification` الإصدار 5، `notify-new-request` الإصدار 11، `notify-new-message` الإصدار 8، و`geocode` الإصدار 1.
- تطابقت بصمة سر webhook بين قاعدة البيانات والدوال؛ لم تُعرض قيمة السر.
- عناوين Auth الحالية تغطي موقع التطبيق ومساراته، و`akhoo://auth-callback` و`akhoo://reset-password`. Google وApple مفعّلان في الإعدادات؛ لم يُنفّذ دخول فعلي بهما.
- قبل النشر: مشرف فعّال واحد، 28 ملف مستخدم، بلا تعيين ذاتي أو تكرار في المهام النشطة. بعد النشر بقي عدد المستخدمين والمشرفين نفسه.

حُفظت لقطة قراءة متسقة قبل التعديل في `supabase/.temp/prelaunch-20260920-snapshot.json` تشمل بيانات 22 جدول تطبيق، و49 دالة، والسياسات والقيود والأعمدة والفهارس والمشغلات والمنح، وإعدادات الحاويات وبيانات تعريف المرفقات الخمسة. الملف مستثنى من Git ويحتوي بيانات خاصة. هذه لقطة مساعدة للاسترجاع، وليست نسخة كاملة مجرّبة تشمل Auth وملفات Storage. لم تُرجع خدمة النسخ الاحتياطية نسخاً متاحة، وتعذّر `pg_dump` لعدم وجود Docker/Podman؛ لم تُنفّذ تجربة استعادة.

## تحديث الهواتف

- نُشر تحديث OTA لمنصتي Android وiOS على قناة/فرع `production`، وruntime `2.0.0`.
- المجموعة: `0c1df634-c7bc-479d-96d2-1a70f9f2cd23`.
- Android: `01a0bf1b-943f-758e-a253-b0afd02012d1`.
- iOS: `01a0bf1b-943f-7ffd-b7a5-be468417c963`.
- [صفحة التحديث في Expo](https://expo.dev/accounts/abad003/projects/sanad/updates/0c1df634-c7bc-479d-96d2-1a70f9f2cd23).
- تم التحقق من ظهور المجموعة كآخر تحديث على فرع الإنتاج، ومن تطابق runtime وSDK مع البناءين الحاليين. أُعيد تجميع الحزم باستخدام متغيرات بيئة EAS production.
- لم تُنشأ نسخة APK/IPA جديدة ولم يُرسل إصدار جديد إلى متاجر التطبيقات. وصول التحديث وتشغيله على هواتف فعلية لم يُختبرا.

## التحقق بعد النشر

- استجابة HTTP 200 للتطبيق وصفحتي الخصوصية واستعادة كلمة المرور، وللإدارة ومساري البلاغات والإشعارات.
- تطابقت بصمة SHA-256 لحزمة JavaScript المنشورة مع البناء المحلي في التطبيق والإدارة.
- الدوال الخمس أعادت 401 للطلبات غير المصرّح بها؛ الدوال الثلاث التي يستدعيها المتصفح أعادت 204 لطلبات CORS OPTIONS.
- حاوية المحادثة أصبحت خاصة، وتعذّر الوصول العام إلى المرفقات الخمسة الموجودة قبل النشر.
- أُلغيت صلاحية bootstrap للمستخدمين، وتأكد وجود الجداول الجديدة للأجهزة والحظر والتشخيص.
- لم يُرجع فحص سجلات Vercel بمستوى error أي سجلات للنشرين خلال نافذة الفحص؛ ذلك لا يثبت خلو جلسات المستخدمين من الأخطاء.
- لم تظهر جلسات Chrome قابلة للتحكم عبر Computer Use، ولذلك لم يُنفّذ فحص بصري بعد النشر. قائمة القبول في `LAUNCH_READINESS.md`، بما فيها تجربة حسابين وجهازين والإشعارات وحذف الحساب، تبقى مطلوبة.

لم تُضبط مراقبة خارجية أو تنبيهات تلقائية في هذه المهمة. تحذير حجم حزمة الإدارة ما زال موجوداً. لا يُنصح بإعادة نسخة الواجهة القديمة بعد جعل المرفقات خاصة؛ عالج أي خلل بإصلاح متوافق مع السياسات الجديدة.

## ملاحظة البيئة المحلية

احتاجت أدوات Node إلى `$env:NODE_USE_SYSTEM_CA='1'` لاستخدام مخزن شهادات Windows الموثوق. بقي التحقق من TLS مفعّلاً؛ لم تُعطّل حماية الشهادات.
