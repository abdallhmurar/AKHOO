# تجهيز النسخة التجريبية المجانية — AKHOO

النطاق المعتمد: التطبيق ولوحة الإدارة، مساعدة مجتمعية مجانية، وعروض/نقاط بإشراف الفريق. الدفع وأدوات الشركاء الذاتية ليست ضمن هذه النسخة. نُشرت تحديثات Supabase والويب والإدارة وتحديث Android/iOS على قناة الإنتاج بتاريخ 20 سبتمبر 2026 بعد طلب النشر. تفاصيل التنفيذ وحدود التحقق في [سجل النشر](DEPLOYMENT_2026-09-20.md). اختبارات القبول اليدوية أدناه تبقى مفتوحة حتى تنفيذها على أجهزة فعلية.

## ما تغيّر

- migrations جديدة `0027`–`0031`؛ ملفات migrations المنشورة سابقاً لم تتغير.
- منع إنشاء طلب بمساعد/حالة مزوّرة، والتوثيق الذاتي للمتطوع، وصلاحيات المشرف المحظور. إغلاق bootstrap العام للمشرف الأول.
- مسح ذاكرة التطبيق والإدارة عند تغيير المستخدم، والتحقق الدوري من صلاحية المشرف.
- مرفقات المحادثة خاصة وروابطها الموقعة مدتها 15 دقيقة. تُحوّل الروابط التاريخية إلى مسارات أثناء migration؛ تبقى الملفات نفسها في مكانها.
- إبلاغ وحظر داخل المحادثة، وإخفاء الرسالة من الإدارة أثناء وجود بلاغ مفتوح، مع سجل للإجراء. الحظر يمنع الرسائل والمطابقة والإشعارات بين الطرفين؛ لا يُلغي المهمة القائمة تلقائياً.
- حذف الحساب يتحقق من JWT ومن تسجيل دخول حديث، ويمسح الملفات عبر Storage API قبل حذف الحساب. OAuth لا يتطلب كلمة مرور غير موجودة.
- روابط استعادة كلمة المرور للويب، وصفحات الخصوصية والشروط العامة، وإضافة الصورة بعد الدخول بدلاً من اشتراط صورة قد تضيع قبل تأكيد البريد.
- إشعارات لكل جهاز: الموافقة/التعطيل والخروج، دفعات بحد أقصى 100، فحص تذاكر Expo، سجل قبول/فشل/نتيجة غير معروفة، وإعادة محاولة من سجل الإدارة دون إنشاء حملة جديدة.
- استقبال النقر على الإشعار، واستعادة هوية مهمة الموقع بالخلفية من التخزين مع مطابقتها للجلسة الحالية.
- البحث عن العنوان بطلب صريح، عبر proxy موحد مع cache وسقف إجمالي طلب واحد كل 1.1 ثانية. يمكن تغيير `GEOCODING_BASE_URL` من إعدادات الدالة دون تحديث الهاتف.
- تشخيص للأخطاء مصنف ومحدود المعدل، دون إرسال نص الخطأ أو المحادثات أو الموقع أو البريد أو مفاتيح الدخول. تُحذف الأحداث الأقدم من 30 يوماً عند ورود أحداث جديدة.
- CI يتحقق من TypeScript للتطبيق والإدارة والدوال، والاختبارات، وبناء الويب والإدارة.

## فحوص محلية

نتيجة التحقق المحلي بتاريخ 20 سبتمبر 2026: نجح 163 اختباراً للتطبيق والأمان والدوال، و29 اختباراً للوحة الإدارة. نجحت فحوص TypeScript للتطبيق والإدارة والدوال، وفحص lint للتطبيق دون ملاحظات. لوحة الإدارة بلا أخطاء lint مع تحذيرين سابقين متعلقين بـ Fast Refresh. نجح بناء الويب ولوحة الإدارة وتجميع حزم Android وiOS؛ ما زال بناء الإدارة يعطي تحذيراً عن حجم الحزمة.

تشغيل من جذر المشروع:

```powershell
npm.cmd run typecheck
npm.cmd run typecheck:edge
npm.cmd run lint
npm.cmd test
npm.cmd run build:web
npm.cmd --prefix admin run typecheck
npm.cmd --prefix admin run lint
npm.cmd --prefix admin test
npm.cmd --prefix admin run build
```

اختبارات PostgreSQL تشغّل سلسلة migrations الفعلية داخل PGlite مع أدوار وصلاحيات RLS، وهوية Supabase وتخزين بديلين للاختبار. لا تتصل بقاعدة البيانات الحقيقية؛ امتدادات الشبكة المستضافة ليست جزءاً من هذه المحاكاة. اختبارات الدوال تشغّل الـ handlers الفعلية مع خدمات بديلة وتمنع الشبكة.

تجميع حزم Android/iOS تحقق من JavaScript/Hermes فقط؛ ليس إنشاء APK/IPA موقّعاً ولا اختبار أجهزة فعلية.

## ترتيب النشر

1. تأكيد المشروع والحساب والبيئة المستهدفة، ونسخة احتياطية لقاعدة البيانات وStorage مع تجربة استعادة على staging. المشروع المشار إليه في المستودع هو `zmpwdufahjmlxfwzafvr`؛ لا تعتمد الاسم وحده لتحديد الإنتاج.
2. تشغيل `supabase/launch-preflight.sql` للقراءة. التأكد من migrations حتى `0026` ووجود مشرف معروف غير محظور. لا تُعد تشغيل `0001` فوق مخطط قديم تم إنشاؤه يدوياً؛ عالج سجل migrations أولاً.
3. مراجعة أي تعيين ذاتي أو نقاط تاريخية مشتبهة. لا تمسح التاريخ آلياً. قيد اختلاف المشاركين أُضيف `NOT VALID` لحماية الكتابات الجديدة مع إبقاء السجلات القديمة للمراجعة. بعد معالجة الحالات المعتمدة، شغّل `ALTER TABLE public.help_requests VALIDATE CONSTRAINT help_requests_distinct_participants`.
4. جرّب migrations الخمس والدوال والعملاء معاً على staging، ثم نسّق نافذة تحديث للمشاركين في التجربة. إغلاق الحاوية العامة سيمنع عرض المرفقات في العملاء القدامى؛ يجب تحديث التطبيق ولوحة الإدارة.
5. اعرض خطة migrations عبر `supabase db push --linked --dry-run` بعد التحقق من الربط، ثم طبّق التغييرات على البيئة المعتمدة. لا تحفظ مفاتيح الخدمة في المشروع أو الواجهة.
6. انشر الدوال `delete-account` و`send-broadcast-notification` و`notify-new-request` و`notify-new-message` و`geocode` لنفس المشروع. إعداد `verify_jwt = false` مقصود: دوال المتصفح تتحقق من المستخدم/صلاحياته داخلها، ودوال webhook تتحقق من السر المشترك.
7. تأكد أن `NOTIFY_WEBHOOK_SECRET` في الدوال يطابق القيمة الخاصة في قاعدة البيانات دون نسخها إلى سجلات العمل. مفاتيح الخدمة تبقى في الدوال فقط.
8. أضف عناوين التحويل الفعلية إلى Supabase Auth: أصل الويب، `APP_ORIGIN/reset-password`، `APP_ORIGIN/account/delete-confirm`، و`akhoo://auth-callback` و`akhoo://reset-password`. تحقق من Google/Apple وإرسال البريد على الحساب الفعلي.
9. انشر بناء الويب والإدارة، ثم وزّع تطبيق الاختبار المحدث. جدول الأجهزة الجديد يُملأ عندما يفتح المستخدم النسخة الجديدة ويسجّل الدخول أو يفعّل الإشعارات؛ لم ننسخ الرموز القديمة دون إعادة مزامنة الموافقة.
10. تحقق من إعدادات APNs/FCM/EAS على أجهزة فعلية. إشعارات الويب غير مضافة؛ الإشعارات في هذه التجربة للهواتف.

## قبول النسخة التجريبية

- [ ] حسابان وجهازان: إنشاء طلب → قبول → في الطريق → وصول → طلب إكمال → تأكيد → نقاط مرة واحدة.
- [ ] رفض تأكيد الإكمال، الإلغاء، تحرير المتطوع، محاولة قبول متزامنة، وانقطاع الشبكة وإعادة فتح التطبيق.
- [ ] حساب عادي ومشرف ثم تبديل الحساب على المتصفح نفسه؛ لا تظهر بيانات الحساب السابق. المشرف المحظور لا يستطيع القراءة أو الإجراء الإداري.
- [ ] رسالة وصورة وفيديو، فتح المحادثة من الإشعار، وفشل إرسال لا يمسح المسودة. الرابط العام القديم لا يُقرأ بعد جعل الحاوية خاصة.
- [ ] بلاغ حقيقي تجريبي → يظهر للإدارة → مراجعة مرفق → إخفاء رسالة → حل البلاغ. منع غير المشاركين من الاطلاع.
- [ ] حظر من الطرفين ثم إزالة حظر أحدهما؛ لا تعود المراسلة ما دام الطرف الآخر يحظر.
- [ ] تعطيل الإشعارات/تسجيل الخروج على جهاز مع بقاء جهاز آخر للحساب، ثم تبديل الحساب على الجهاز الأول.
- [ ] قبول Expo ليس إثبات وصول للهاتف. اختبر الوصول والنقر وحالة الإذن المرفوض والموقع بالخلفية بعد إغلاق التطبيق.
- [ ] حملة اختبار ذات فشل جزئي؛ تبقى حالتها غير مكتملة. لا يُعاد إرسال النتائج غير المعروفة تلقائياً لتجنب التكرار؛ يلزم فحصها لدى المشغّل.
- [ ] استعادة كلمة مرور عبر البريد على الويب والموبايل. حذف حساب تجريبي يحوي صوراً، وحساب OAuth بعد إعادة الدخول.
- [ ] العربية والعبرية والإنجليزية، اتجاه الواجهة، لوحة المفاتيح، أحجام الهواتف والنص الكبير. الوثائق القانونية الحالية بالعربية؛ اعتماد الترجمة القانونية ما زال عملاً منفصلاً.
- [ ] عرض العروض/النقاط واستبدالها عبر الإدارة دون دفع. إبلاغ الشركاء بأن أدوات الخدمة الذاتية ليست متاحة في التجربة.
- [ ] تأكيد نطاق مناطق التجربة وأرقام الدعم؛ ملف النص القانوني يحتوي أرقام اتصال مختلفة عن شاشة الدعم، وهي من المحتوى السابق ولم نغيرها دون اعتماد.

## متابعة التشغيل

```sql
-- توزيع أخطاء العملاء خلال اليوم، دون نصوص أو معلومات اتصال.
select code,domain,operation,count(*) from public.client_error_events
where created_at>now()-interval '1 day' group by 1,2,3 order by count(*) desc;

select delivery_status,count(*) from public.broadcast_notifications group by 1;
select status,count(*) from public.reports group by 1;
```

يحتاج الفريق مسؤولاً للبلاغات ومراقبة سجلات Edge وتنبيهات الانقطاع وخطة استعادة مجرّبة. لم تُضبط حسابات تنبيه أو خدمات مراقبة خارجية في هذه المهمة. عند التراجع لا تعِد فتح حاوية المحادثة للعامة ولا تُعد صلاحيات bootstrap أو سياسات الإدخال القديمة؛ عالج المشكلة بإصلاح إضافي أو إيقاف التجربة مؤقتاً.

مراجع التنفيذ: [مصادقة JWT](https://supabase.com/docs/guides/auth/jwt-fields)، [رفع ملفات React Native](https://supabase.com/docs/reference/javascript/storage-from-upload)، [سياسة Nominatim](https://operations.osmfoundation.org/policies/nominatim/).
